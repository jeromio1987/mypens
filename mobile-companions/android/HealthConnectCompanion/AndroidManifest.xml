<?xml version="1.0" encoding="utf-8"?>
<!--
    Reference AndroidManifest.xml for the Health Connect companion stub.
    Merge the <uses-permission> and <queries> entries into your project's
    own manifest. The <activity> below assumes the Empty Activity template.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthconnectcompanion">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <!--
        Android 14+ only. Lets the WorkManager periodic worker read new
        ExerciseSessionRecords while the app is in the background. Older
        platforms ignore this permission silently.
    -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:label="My Pens HC"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Required so the Health Connect rationale screen can find us. -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>
    </application>
</manifest>
